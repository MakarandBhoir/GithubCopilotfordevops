provider "azurerm" {
  features {}

  # Optional: Uncomment and set the following if you need to specify a subscription ID
  # subscription_id = "your-subscription-id"

  # Optional: Uncomment and set the following if you need to specify a tenant ID
  # tenant_id = "your-tenant-id"

  # Optional: Uncomment and set the following if you need to specify a client ID and secret for service principal authentication
  # client_id     = "your-client-id"
  # client_secret = "your-client-secret"
}